# ESIS v2 API бэлэн байдлын матриц

**Шалгасан огноо:** 2026-09-08

**Албан ёсны эх:** <https://developerv2.esis.edu.mn/api/structure>

**Hub:** `https://hubv2.esis.edu.mn`

Developer portal-ийн public catalog-д нийт **198 сервис** байна: 119 `GET`,
79 `POST`. NomadKids бүх сервисийг хүсэхгүй. А/261-ийн мэдээлэл солилцоо болон
СӨБ-ын шаардлагыг хангахад шууд хэрэгтэй **17 сервисийг** хамгийн бага эрхийн
зарчмаар сонгож backend adapter-т оруулсан.

Portal-ийн 2026-03-31-ний мэдэгдэл v1 сервисүүдийг 2026-04-01-нээс хааж эхлэхийг
зарласан. Иймээс энэ матриц зөвхөн `/hub/v2/` зам ашиглана.

## 1. “Бэлэн” гэдэг үгийн утга

| Түвшин         | Утга                                                                                     |
| -------------- | ---------------------------------------------------------------------------------------- |
| **C1 Catalog** | ESIS v2 catalog-д API ID, method, URL, талбар, sample байна                              |
| **C2 Code**    | Endpoint registry, Zod validation, domain method, unit test бэлэн                        |
| **C3 Access**  | Манай `ACCESS_TOKEN`-д тухайн API ID зөвшөөрөгдсөн                                       |
| **C4 Test**    | ESIS test орчинд бодит байгууллагын өгөгдлөөр амжилттай шалгасан                         |
| **C5 Live**    | Баталгаатай import, external ID, BullMQ retry/idempotency, attendance reconcile ажиллана |

Одоогийн төлөв: сонгосон 17 сервисийн **C1 + C2 код бүрэн холбогдсон**. Tenant
mapping, operator UI, read-only dry-run, `EsisSyncRun` audit history, багш/ажилтны
live profile, хүүхдийн live template, ирцийн live ID тулгалт ба attendance POST
ажиллана. Official Bearer token тавихад live горим автоматаар нээгдэнэ.
★ **C3 нээгдсэн — 2026-09-14.** Энэ мөр «C3-C5 нотолгоо нь БМТТ-өөс token
scope олгох хүртэл хүлээгдэнэ» гэж байсан бөгөөд C3-ийн хувьд худал болсон:
`ESIS_TOKEN` нь **institutionId 42778** (Дэгдээхий үрс цэцэрлэг, Улаанбаатар,
Баянзүрх) дээр эрхтэй. Шалгасан хариултууд:

| Хүсэлт                                   | Хариу                                             |
| ---------------------------------------- | ------------------------------------------------- |
| `organization/info?institutionId=42778`  | `200` — байгууллагын бодит бичлэг                 |
| `group/list`, `students/list`            | `200` — 4 бүлэг, 83 суралцагч                     |
| `teacher/list`, `school/staff`           | `200` — 10 багш, 13 ажилтан                       |
| `building/list`, `room/list`             | `200` — 1 байр, 5 өрөө                            |
| Authorization header-гүй                 | `401` — токен хүчинтэй гэдгийг батлана            |
| `institutionId=1`, `10001`, `40305`      | `403` — эрх нь нэг institution-оор хязгаарлагдсан |
| `POST stdnt/all/contacts` + `{personId}` | `200` — нэг хүүхдийн асран хамгаалагчийн бичлэг   |

★★ Сүүлийн мөр 2026-09-14-нд **зассан**. Өмнө нь «`stdnt/all/contacts` → `404`,
асран хамгаалагчийн сервис энэ scope-д алга» гэж байсан. Сервис байгаа: `GET`
дээр 404 өгдөг, `POST` дээр ажилладаг, мөн body-д `personId` шаардана
(эс бөгөөс `400 personId шаардлагатай`). 83 хүүхдээс 2 нь бичлэгтэй.

### 1.1 C4 — эхний бодит тулгалт, 2026-09-14

Яамны өөрийн зөвшөөрөгдсөн сервисийн жагсаалт (`apis-granted.xlsx`, 84 мөр,
API_ID + method + URL) болон **амьд хариунуудтай** каталогийг тулгав.

| Шалгасан зүйл                             | Дүн                                                             |
| ----------------------------------------- | --------------------------------------------------------------- |
| Холбогдсон 40 сервисийн зам ба method     | **40/40 таарсан** — зөрүүгүй                                    |
| Хүсэлтийн бүртгэл (`esis.requests.ts`)    | 84 зөвшөөрөгдсөн ID бүгд байна; илүү 13 нь PENDING/CANCELLED    |
| Уншигчийн schema-г амьд хариутай тулгасан | **36-аас 9 нь буруу** — 2 нь бүр алдаа өгдөг, 7 нь утгыг хаядаг |
| Хоосон хариу (`203`)                      | 3 өөр хэлбэр: `RESULT: ""`, `RESULT: []`, `RESULT` огт байхгүй  |

Эндээс гарсан засварууд:

- `esisListParser` хоосон хариуг «гэрээ зөрчсөн» биш «мэдээлэл алга» гэж
  уншина. Өмнө нь `groupsNextYear` энэ шалтгаанаар амьд орчинд унаж байсан.
- `rooms` (`roomId` → `facilityId`) ба `academicOrg`
  (`academicOrgId` → `subjectDepartmentId`) шаардлагатай талбар нь буруу байсан
  тул бүтэн сервис `invalid_response` өгдөг байв.
- `teacherAcademicOrg`, `teacherMovements`, `studentCondition`,
  `studentStatistics`, `programs`, `subjectAreas`, `students` — зохиосон
  талбарууд нь бүгд nullable байсан тул parse хийгдээд утга нь чимээгүй
  хаягддаг байлаа. Дэлгэц хоосон багана зурж, «энэ байгууллагад мэдээлэл алга»
  мэт харагдана.
- `studentCheck` бол объект биш, `"true"` гэсэн ганц утга. `studentContacts` бол
  бүх бүртгэлийнх биш, **нэг хүүхдийн** лавлагаа.
- `fieldSource` дээр гурав дахь утга `LIVE` нэмэгдсэн: `PORTAL` = яам баримтжуулсан,
  `LIVE` = яам **илгээсэн**, `ADAPTER` = хоёуланг нь шалгаагүй.

### 1.1.1 27 сервис нэмж холбов — 2026-09-15

Каталог **40 → 67 сервис** болов. Нэмэгдсэн 27:

| Бүлэг              | Сервис                         | Амьд шалгалт     |
| ------------------ | ------------------------------ | ---------------- |
| Эрүүл мэнд унших   | 97, 98, 96, 95, 99, 94, 93     | 7/7 ажиллав      |
| Эрүүл мэнд бичих   | 77, 76, 78, 79, 75, 81, 82, 84 | орц батлагдаагүй |
| Вакцин             | 62, 63, 64                     | 3/3 ажиллав      |
| Бүлгийн хэмжилт    | 109, 108                       | 109 — 18 мөр     |
| Эрт илрүүлэг       | 195, 194, 190                  | 195 — 25 асуулт  |
| Багш бүртгэх       | 49, …800, …781                 | 3/3 ажиллав      |
| Ирцийн нэгдсэн дүн | 11                             | 4 бүлгийн мөр    |

17 шинэ уншигчийг амьд ЭСИС рүү бодит үйлчилгээний кодоор дуудаж шалгасан —
**алдаагүй**. `teacherProfile` 20/20 талбар, `schoolAttendance` 12/15,
`studentAssessments` 2 мөр, `workerInfo` дээр `civilId`, `personRegNumber`
хоёулаа таслагдсаныг баталсан.

### 1.1.2 Гэрээ нь тодорхойгүй сервисүүд

Зургаан уншигч (`97`, `98`, `96`, `94`, `93`, `194`) институт 42778-ийн бүх 83
хүүхдэд `203` буцаадаг тул **гаралтын талбарууд нь хэзээ ч харагдаагүй**.

Тэдгээрт зохиосон талбарын жагсаалт бичээгүй. Оронд нь дамжуулах схем
(`esisDiscoveredSchema`) ашиглаж, дэлгэц нь **эхний бодит хариунаас** баганаа
уншина (`ESIS_DISCOVERED_SHAPE`, `esisFieldsFor`). Ингэснээр §1.1-д бичсэн
«зохиосон талбар» алдааг давтахгүй бөгөөд бодит бичлэг гарч ирэхэд ЭСИС-ийн
өөрийнх нь нэрсийг утгатай нь харна.

★ Дамжуулах схем дээр «нэрлээгүй учраас авахгүй» гэдэг хамгаалалт ажиллахгүй
тул `civilId`, `personRegNumber`, нууц үгсийг **кодоор** таслана
(`ESIS_REFUSED_FIELDS`), мөн үүнийг тестээр баталгаажуулсан.

### 1.1.3 Вакцины сервис тасалданги

`vaccine/history/9129027526058` нэг хүүхдэд 17 бодит вакцин буцаасан бөгөөд
нэг цагийн дараа яг тэр хүүхдэд `203` буцаав — ижил токен, ижил байгууллага.

Тиймээс хоосон хариуг **«вакцин хийлгээгүй» гэж ойлгож болохгүй**. Энэ бол
`foodDiscountStudents`-ийн дүрэм, гэхдээ илүү хурцаар: хариу өгөөгүй сервисээс
хүүхдийн талаарх эмнэлгийн дүгнэлт гаргах болно. Дэлгэц «мэдээлэл ирсэнгүй»
гэж хэлнэ.

**C4 бүрэн биш**: 17 сервис хараахан холбогдоогүй (иргэний бүртгэлийн дугаар
шаардсан, давхардсан байж болзошгүй бичих сервис, улсын бүртгэлийн дугаарын
багана байхгүй, ZEREG өөр домэйн, URL нүд хоосон), мөн бичих сервисүүдийн
орцыг унших замаар мэдэх боломжгүй. **C5 хэвээр хүлээгдэнэ.**

### 1.1.4 Хүүхэд тус бүрийн уншилтын эрх — 2026-09-15

`assertReadable` нь хоёр л зүйл шалгадаг байв: дуудагч энэ түрээслэгчийн гишүүн
үү, түүний эрх энэ сервист хүрэх үү. **Аль нь ч хүүхдийн тухай биш.**

Хүүхэд тус бүрийн ESIS сервис бүр `personId`-аар хаяглагддаг бөгөөд `students`
нь бүх roster-ын дугаарыг ямар ч багшид өгдөг. Тиймээс **1-р бүлгийн багш
2-р бүлгийн хүүхдийн харшил, хөгжлийн бэрхшээл, хэмжилтийг уншиж чадна**
байсан — CLAUDE.md §4.1-ийн эхний тохиолдол.

Энэ нь 2026-09-10-нд нэмэгдсэн `studentStatistics`, `studentCondition`,
`studentCheck`-д аль хэдийн байсан цоорхой; 2026-09-15-нд эрүүл мэндийн 11
сервис нэмэгдэхэд цар хүрээ нь эрс өргөжсөн тул хаав.

**Яагаад шууд засагдаагүй вэ:** `Child`-д ESIS `personId` багана байгаагүй тул
`canAccessChild`-ыг `personId`-д хэрэглэх зам огт байхгүй байв.

| Юу нэмэгдсэн                                                         | Хаана                                        |
| -------------------------------------------------------------------- | -------------------------------------------- |
| `Child.esisPersonId` багана, `(kindergartenId, esisPersonId)` unique | `20260915090000_child_esis_person_id`        |
| `personId` → `Child` тайлагч                                         | `EsisRepository.findChildIdByEsisPersonId`   |
| Эрхийн хаалт                                                         | `EsisAdminService.assertCanReadEsisChild`    |
| Тулгалтыг санах                                                      | `AttendanceRepository.rememberEsisPersonIds` |

**Дүрэм:**

- **ADMIN чөлөөлөгдөнө.** `isAdminOver` нь тухайн цэцэрлэгийн бүх хүүхдэд
  зөвшөөрдөг тул хаалт «тийм»-ээс өөр хариулт өгөх боломжгүй. Ажиллуулбал
  нэмэлт query болох ба, илүү дор нь, ESIS дугаар нь тулгагдаагүй хүүхдийг
  admin-д хориглох болно — дүрэм өөрөө зөвшөөрч байгаа хандалтыг татгалзана.
- **Тулгагдаагүй бол татгалзана.** `esisPersonId` нь зөвхөн бүтээгдэхүүн амьд
  roster-тай нэр, төрсөн огноогоор тулгалтыг баталсан газарт бичигддэг
  (`resolveAttendanceDrafts`). Дугааргүй хүүхдийг хэнийх болохыг тогтоох
  боломжгүй, «хэнийх нь мэдэхгүй» гэдэг нь харуулах шалтгаан биш.
- **403 биш 404** (§1.7) — дуудагч тухайн дугаар байгаа эсэхийг ч мэдэхгүй.

Мандат 6 тест `test/esis-admin.test.ts`-д, бодит маршрутаар HTTP-ээр.

### 1.1.5 Дамжуулах схем үндсэн зам болов — 2026-09-15

52 уншигч бүрийг бодит үйлчилгээний кодоор (`EsisService.read()`, түүхий
хүсэлт биш) институт 42778 дээр **нэг удаа** дуудав — `scripts/esis-probe.ts`.
Хүүхэд тус бүрийн сервисийг нэг хүүхдээр шалгасан: 94 хүүхдээр шүүрдэх нь
А/465 §4-ийн «хамгийн бага эрх»-тэй зөрчилдөх лог үлдээнэ.

| Хэмжилт              | Задарсан | Хоосон (203) | **Алдаа** |
| -------------------- | -------- | ------------ | --------- |
| Эхний, засварын өмнө | 32       | 16           | **4**     |
| Эцсийн               | 33       | 19           | **0**     |

Эхний дөрвөөс гурав нь probe-ийн буруу параметр байв — **багшийн** сервист
**хүүхдийн** `personId` дамжуулсан. Энэ нь тэмдэглэх учиртай: бүтээгдэхүүний
кодод ижил алдаа гарвал эвдэрсэн уншигчтай яг адилхан харагдана.

Үлдсэн нэг нь жинхэнэ: `studentInfo` (48) нь `dateOfBirth`-ийг схемд `string`
гэж бичсэн байхад ЭСИС **тоо** илгээдэг тул бүх хүүхдэд `invalid_union`
өгдөг байв.

Эндээс гарсан бүтцийн шийдвэр: **гараар бичсэн схем бол онцгой тохиолдол.**
TypeScript код талбарын нэрийг нь уншдаг долоон уншигчид л үлдэнэ; бусад 45
нь `esisDiscoveredSchema`-аар ЭСИС-ийн илгээснийг хэвээр дамжуулна. Ингэснээр
«ЭСИС илгээсэн ч бид нэрлээгүй учраас чимээгүй хаясан» алдаа бүтцээрээ
боломжгүй болно — §1.1-д бүртгэгдсэн 36-аас 9 нь яг тэр байдлаар алдагдсан.

**Хоосон хариу: дөрөв дэх хэлбэр, гурван parser.** `teacher/movements` нь
`HTTP 205`-ыг **тэг байт** биетэйгээр буцаадаг. `EsisClient` түүнийг `null`
болгон дамжуулдаг бөгөөд `z.object()` нь `null`-ыг шууд татгалздаг тул
«энэ сард хэн ч шилжээгүй» гэсэн хэвийн хариу `invalid_response` болж
хувирдаг байв.

Энэ дүрмийг **гурван удаа** бичих шаардлагатай болсон, учир нь parser бүр
өөрийн envelope барьдаг:

| Parser               | Юу илрүүлсэн                                               |
| -------------------- | ---------------------------------------------------------- |
| `esisListParser`     | `teacher/movements` — 205, хоосон бие                      |
| `esisCheckParser`    | `teacher/check` — энэ институтэд багш биш `personId`-д 205 |
| `esisContactsParser` | 83 хүүхдээс **2** нь л бичлэгтэй; хоосон нь ердийн хариу   |

Гурав дахь нь хамгийн чухал: асран хамгаалагчийн мэдээлэл байхгүй байх нь энэ
сервисийн **ердийн** хариу болохоос онцгой тохиолдол биш.

**Танигчийн төрөл, амьдаар хэмжсэн.** Клиент 2026-09-15-нд «РД-г тийм, нууц
үгийг үгүй» гэж шийдсэний дараа схемүүдэд `civilId`-г `nullableString` гэж
нэмэхэд **94/94 мөр дээр roster бүхэлдээ унав**. Хэмжилт (зөвхөн төрөл, тоо):

| Сервис                         | `civilId`        | `personRegNumber`            |
| ------------------------------ | ---------------- | ---------------------------- |
| `students` (94)                | **number** 94/94 | string 94/94                 |
| `school/staff` (13)            | **number** 13/13 | string 13/13                 |
| `teacher/list` (10)            | **number** 10/10 | string 10/10                 |
| `cook/levelHood/students` (72) | **number** 72/72 | — (`registerNumber`: string) |

`civilId` нь `nullableIdentifier` болов — `z.union([string, number])` нэг
хэлбэрт хөрвүүлнэ. `personRegNumber` нь `nullableString` хэвээр, учир нь
монгол регистр (`УБ12345678`) үсэг агуулдаг тул JSON тоо болж чадахгүй.

★ **Энэ бүгдийг 2228 тест барьж чадахгүй.** `test/setup.ts` нь `ESIS_TOKEN`-ийг
устгадаг тул suite доторх бүх ESIS маршрут stub дээр ажилладаг. Тестийн иж
бүрдэл ногоон байх нь уншигчид яамны илгээснийг зөв задалж байгаагийн
нотолгоо **биш**. Амьд probe нь тусдаа алхам бөгөөд schema эсвэл parser
хөндсөн өөрчлөлт бүрийн дараа ажиллуулах ёстой.

### 1.1.6 Ажилтны өөрөө бүртгүүлэх урсгал — 2026-09-16

Багш өөрөө **цэцэрлэгийн код + регистрийн дугаараар** бүртгүүлнэ. Захирал
батлахгүй, зөвхөн хянана (захиалагчийн заавар). Бүртгэлийн маршрут нь
нэвтрээгүй хүнд нээлттэй тул **ЭСИС рүү хэзээ ч хандахгүй** — оронд нь
ADMIN-ы дүүргэсэн `EsisStaffRoster` хүснэгтийг уншина.

**Roster бодит ЭСИС-ээс:**

| Хэмжилт                   | Дүн    |
| ------------------------- | ------ |
| `school/staff` мөр        | 13     |
| Хадгалсан                 | **13** |
| Алгассан (РД уншигдаагүй) | **0**  |
| `isInstructor` үнэн       | 9      |

**Үүрэг нь `jobCode`-оос гарна** (ISCO-08), `positionName`-ээс биш — сүүлийнх нь
чөлөөт текст бөгөөд 13 мөр дотроо толгой үсгээрээ зөрчилддөг («ахлах Тогооч»,
«туслах Тогооч»).

| jobCode              | Албан тушаал         | Тоо | Үүрэг             |
| -------------------- | -------------------- | --- | ----------------- |
| `2342-13`, `2342-05` | Багш, багшийн туслах | 8   | `TEACHER`         |
| `2351-01`            | Арга зүйч            | 1   | `TEACHER`         |
| `5120-11`            | Тогооч               | 2   | `COOK`            |
| `5153-12`            | Жижүүр               | 1   | **үүрэг байхгүй** |
| `1341-02`            | Эрхлэгч              | 1   | **үүрэг байхгүй** |

Сүүлийн хоёр нь **зориудаар**. Жижүүрт `TEACHER` өгөх нь хамгийн түгээмэл
үүрэг учраас таарах мэт боловч байрны манаачид хүүхэд бүрийн хөгжлийн
бичлэгийг өгнө. Эрхлэгчид `ADMIN` яг таарна — батлах алхам байхгүй тул өөр
байгууллагын мэдээллийн сан дахь мөр цэцэрлэгийг хэн удирдахыг шийдэх болно.
`ADMIN`, `ACCOUNTANT` хоёр зөвхөн урилгаар үлдэнэ.

**Хоёр олдвор, аль аль нь амьд хэмжилтээс:**

1. `school/staff` нь РД-г **жижиг үсгээр** илгээдэг — 13-аас 0 нь загварт
   тохирч, том үсэг болгоход 13/13 тохирно. `teacher/list` нь яг тэр 13 хүний
   дугаарыг том үсгээр илгээнэ. Roster нь `school/staff`-аас баригддаг тул
   **хэвийн болгосон утгыг хадгална**; түүхийг нь хадгалбал ямар ч ажилтан
   хэзээ ч таарахгүй бөгөөд дэлгэц «та жагсаалтад байхгүй» гэж хэлнэ.
2. `teacher/list` нь **нэг хүнийг давхардуулдаг** — 10 мөр, ердөө 9 ялгаатай
   `personId`, ялгаа нь зөвхөн `username`. Тэндээс roster барихад
   `@@unique([kindergartenId, esisPersonId])` зөрчигдөж гүйлгээ унана. Энэ нь
   `school/staff` сонгосны хоёр дахь, бие даасан шалтгаан.

**Бүрэн туршилт, бодит өгөгдөл дээр:**

| Алхам                               | Дүн                                            |
| ----------------------------------- | ---------------------------------------------- |
| Roster татах                        | `200 {count:13, skipped:0}`                    |
| Жинхэнэ багш бүртгүүлэх (`2342-13`) | `201`, урилгын token                           |
| Данс                                | `TEACHER`, ЭСИС-ийн нэрээр, санамсаргүй handle |
| Нууц үг тохируулах                  | `204`                                          |
| Шинэ нууц үгээр нэвтрэх             | `200`, `role: TEACHER`                         |

**Татгалзлууд амьд өгөгдөл дээр ялгагдахгүй.** Дөрвөн өөр шалтгаан —
код буруу, roster-т байхгүй, дугаар хэлбэр буруу, жижүүр (roster-т **байгаа**,
код нь **зөв**) — дөрвүүлээ яг ижил бие буцаав:

```
401 {"detail":"Код эсвэл регистрийн дугаар буруу байна.",
     "status":401,"title":"Нэвтрэх шаардлагатай","type":"about:blank"}
```

Хэрэв эдгээр ялгаатай байсан бол регистрийн дугаарын жагсаалт барьсан хэн ч
«энэ хүн энэ цэцэрлэгт ажилладаг уу?» гэдгийг асууж чадна — тийм жагсаалт
хэд хэдэн газар цаасан дээр байдаг.

★ Roster хоосон эсвэл 30 хоногоос хуучин бол бүртгэл **татгалзана**, амьд
дуудлага руу унахгүй. Хуучирсан roster нь гарсан хүмүүсийг жагсаасан хэвээр
байдаг — яг тэд данс авах ёсгүй.

### 1.1.7 Гурван давхаргат sync — товлогдсон уншилт хэзээ ч хүүхдэд хүрдэггүй нь батлагдсан — 2026-09-17

`2026-09-16-esis-sync-tiers.md` төлөвлөгөө ESIS-ээс уншихыг гурван давхаргад
хуваасан:

| Давхарга | Давтамж      | Юу                                                             | Хаана                                       |
| -------- | ------------ | -------------------------------------------------------------- | ------------------------------------------- |
| 1        | Сар бүр      | Лавлах каталог (хоол, байр, өрөө, программ…) → `EsisReference` | `REFERENCE_RESOURCES` (`esis.reference.ts`) |
| 2        | Өдөр бүр     | Ажилтны roster + `studentMovements` тоолол                     | `ROSTER_RESOURCES` (`esis-sync.service.ts`) |
| 3        | Дуудалт бүрд | Хүүхэд тус бүрийн унших, `canAccessChild`-аар хаалттай         | `EsisAdminService.read`                     |

**3-р давхарга шинээр бичигдээгүй** — энэ бол §1.1.4-т аль хэдийн баталсан
зам (`assertCanReadEsisChild` → `AuditLog` `VIEW` мөр). Энэ ажил зөвхөн
баталгааг батлаад бичив:

1. **Хүүхэд тус бүрийн унших нь яг нэг `AuditLog` мөр үлдээдэг, тэр мөр
   дуудагч, унших зүйл, хэний бичлэгийг нэрлэдэг** — `actorUserId`, `objectId`
   (сервисийн нэр), `metadata.params.personId`. Тест:
   `apps/api/test/esis-admin.test.ts` → _"leaves exactly one AuditLog row
   naming the actor, the child and the resource"_.

2. **Хамгийн чухал баталгаа: товлогдсон буюу гараар эхлүүлсэн ямар ч sync
   хүүхэд тус бүрийн сервист хүрч чадахгүй.** `1`, `2`-р давхаргын бүх
   сервисийг (`REFERENCE_RESOURCES` ∪ `ROSTER_RESOURCES`) нэгтгээд, алийг нь ч
   `personId` авдаггүйг шалгадаг тест:
   `apps/api/test/esis-sync.test.ts` → _"no scheduled sweep reaches a
   per-child resource"_ (1-р давхаргын хувилбар нь
   `apps/api/src/integrations/esis/esis.reference.test.ts`-д аль хэдийн байсан).

   Анхны хувилбар зөвхөн `endpoint.path`-д `:personId` байгаа эсэхийг
   шалгадаг байсан — энэ **хангалтгүй**: `studentContacts` бол `personId`-г
   замдаа биш, **JSON body-гоороо** (`bodyParams`) авдаг, мөн `esis.service.ts`-ийн
   өөрийнх нь тайлбараар энэ бол каталогийн хамгийн их хувийн мэдээлэл
   агуулсан сервис. Тиймээс шалгалт `params` болон `bodyParams`-ыг хамтад нь
   үздэг хэлбэрт шилжсэн.

3. **Хамгаалалт бодитоор эвдэрвэл унадаг эсэхийг шалгасан.**
   `REFERENCE_RESOURCES`-д `studentAllergy` (`personId` авдаг сервис)-г түр
   нэмээд ажиллуулахад гурван тест унасан (`esis.reference.test.ts`-ийн хоёр,
   `esis-sync.test.ts`-ийн нэг). `ROSTER_RESOURCES`-д ижил зүйлийг түр нэмэхэд
   `esis-sync.test.ts`-ийн _"touches no reader that takes a personId, in the
   path or the body"_ тест унасан. Хоёуланг нь буцаагаад арилгахад бүгд
   дахин амжилттай болсон.

### 1.1.8 Хоёр давхаргыг амьдаар ажиллуулав — 2026-09-17

Тестийн stub биш, бодит маршрутаар, амьд яам руу, институт 42778 дээр.

**Лавлахын шүүрдэлт** — `POST …/esis/sync {"tier":"REFERENCE"}` → `200`,
`SUCCEEDED`, **13/13 нөөц, 2689 мөр, нэг секундэд**:

| Нөөц                               | Мөр  | Хамрах хүрээ |
| ---------------------------------- | ---- | ------------ |
| `foodProducts`                     | 1000 | улсын        |
| `foodProductMaterials`             | 1000 | улсын        |
| `foodMaterials`                    | 294  | улсын        |
| `subjectAreas`                     | 287  | байгууллагын |
| `foodMaterialGroups`               | 40   | улсын        |
| `vaccineCatalog`                   | 26   | байгууллагын |
| `screeningQuestions`               | 25   | улсын        |
| `foodProductTypes`                 | 6    | улсын        |
| `rooms`                            | 5    | байгууллагын |
| `academicYearStatuses`, `programs` | 2    | байгууллагын |
| `academicOrg`, `buildings`         | 1    | байгууллагын |

★ Энэ нь `c6fa6f3`-д тэмдэглэсэн эрсдэлийг **хаав**: шүүрдэлт 13 нөөцийг
**зэрэг** уншдаг, хоёр нь мянган мөртэй, харин өмнөх бүх probe дараалан явсан
тул яам 13 зэрэг хүсэлтийг хэрхэн хүлээж авахыг хэн ч хараагүй байв. Хариу:
ямар ч бэрхшээлгүй.

**Roster** — `{"tier":"ROSTER"}` → `200`, `SUCCEEDED`, **13 ажилтан хадгалав,
0 алгасав**, шилжилт хөдөлгөөн 20 бичлэг. `beginDate` нь `2026-09-10` —
долоо хоногийн нөөц утга биш, өмнөх амжилттай ажиллалтаас гарсан.

**Хүүхдийн сервист хүрээгүй — бодит өгөгдлөөр батлав.** Хоёр ажиллалтын
`EsisSyncRun.resources`-д бүртгэгдсэн **бодитоор уншсан** 16 нөөцийг уншигч
бүрийн `params`, `bodyParams`, замтай нь тулгав:

```
run 1 (roster):     staff, teachers, studentMovements
run 2 (reference):  13 лавлах нөөц
PASS — 0 per-child resource(s) across 2 live runs
```

Энэ бол кодоос гаргасан дүгнэлт биш: `resources` нь ажиллалт **юу хийснийг**
хадгалдаг тогтвортой бичлэг тул лог устсан ч аудит хийгдэнэ.

### 1.1.9 Бүлгийн бичих гурвын гэрээ — яамны өөрийнх нь татгалзлаас — 2026-09-18

Spec №3б. Портал эдгээрийг баримтжуулаагүй, яамны export нь зөвхөн id, method,
URL агуулдаг. Тиймээс гэрээг **сервисүүдээс нь өөрөөс нь** асуув —
`studentContacts`-ийн `{ personId }`-г 2026-09-14-нд олсон тэр аргаар.

**150 (үүсгэх) рүү хэзээ ч хандаагүй.** Татгалзсан `update` юу ч үүсгэхгүй;
хагас зөв `create` нь **амжилттай болж**, хэн ч хүсээгүй бүлэг үлдээж
магадгүй. `scripts/esis-group-write-probe.ts` нь 152, 162 хоёрыг л мэднэ.

| Илгээсэн бие                            | Хариу                                                              |
| --------------------------------------- | ------------------------------------------------------------------ |
| `{}`                                    | `institutionId дутуу байна`                                        |
| `{institutionId}`                       | `event утга буруу байна`                                           |
| 152 `{…, event:"update"}` / `"delete"`  | `Хичээлийн жил шалгана уу.`                                        |
| 152 `{…, event:"create"/"save"/"edit"}` | `event утга буруу байна`                                           |
| 152 `{…, event, academicYear:"2026"}`   | `0 ID-тай бүлгийн мэдээлэл олдсонгүй`, `RESULT.studentGroupId:"0"` |
| 162 `{…, event:"update"}` (жижиг үсэг)  | `Үйлдлийн утга буруу байна. (CREATE, UPDATE, DELETE)`              |
| 162 `{…, event:"UPDATE"}` (том үсэг)    | `Багшийн хариуцах үүрэг оруулна уу.`                               |

**Гурван зүйл тогтоогдов:**

1. **152 нь зөвхөн `update` ба `delete` гэсэн хоёр `event`-тэй.** `create`
   татгалзсан. «Бүлэг засах, устгах» гэдэг нь яг энэ — нэг зам, хоёр үйлдэл.
2. **152 нь жижиг үсгээр, 162-ын процедур нь ТОМ үсгээр** хүсдэг. Нэг
   гарцын хоёр давхарга өөр өөр дүрэмтэй.
3. `studentGroupId` нь 152-ын шаардлагатай талбар — хариу нь `"0"` гэж
   цуурайтаж өөрөө нэрээ хэлсэн.

#### Бүлгийн жинхэнэ бүтэц — api-40 (`groups`), яамны өөрийн мөрүүд

42778 дээр **4 бүлэг**, бүгд `academicYear:"2026"`, бүгд `instructorId:null`.

`academicLevel`: **Бага 15 · Дунд 16 · Ахлах 17 · Бэлтгэл 18**

★ Энэ нь `AgeBand`-ын дугаарлалт **биш**. Spec №3б анх `NURSERY..SENIOR → 1..4`
гэж зурагласан нь таамаг байсан бөгөөд **буруу**. Түүнчлэн `programStageId`
түвшин бүрт өөр (…359/…360/…361/…362) тул хатуу хүснэгт нь ижил таамгийг
нэг шат далдуур давтана. Зөв эх сурвалж нь **api-40-ийн мөр өөрөө**.

Бүх бүлэгт нийтлэг: `programOfStudyId` 100000287145352, `programPlanId`
100000287145358, `groupTypeCode` STREAM, `groupShiftId` 108004001,
`groupClassificationId` 1, `groupCategoryCode` MAIN_STUDENT_GROUP,
`academicGroupId` 42778.

`instructorId` нь бүлгийн мөрөн дээр байна — 162 яг түүнийг тавьдаг.

#### Эхний бодит `create` — татгалзсан, юу ч үүсээгүй

`scripts/esis-group-write-exercise.ts`, 2026-09-18. Бүлгийн тоо **өмнө 4, дараа
4** — нэг ч бичлэг үүсээгүй.

```
CREATE payload {"institutionId":42778,"event":"create","academicYear":"2026",
  "studentGroupName":"ЗЗЗ туршилт","academicLevel":"17", …}
CREATE → 400 {"RESPONSE_MESSAGE":"Анги бүлгийн нэрийг 5 буюу түүнээс багаар өгнө үү!"}
```

Хоёр зүйл тогтоогдов:

1. **150-ийн `event` нь `create`** — дискриминаторыг давж, нэрийн шалгалт
   хүртэл явсан.
2. **Бүлгийн нэр 5 тэмдэгтэд багтана.** Хаана ч баримтжаагүй дүрэм. Манай
   `Group.name` нь чөлөөт текст тул «Дэлбээ» (6 тэмдэгт) гэх энгийн нэр
   татгалзана. `ESIS_GROUP_NAME_MAX = 5` болж **бэлтгэх үед** шалгагдана —
   захирал батласныхаа дараа биш.

★ Яамны өөрийн дөрвөн бүлэг энэ дүрмийг зөрчиж байна («ахлах бүлэг» = 11).
Дүрэм нь орж ирэх өгөгдөлд хамаарна, хадгалагдсанд нь биш.

★★ Энэ бол эхний бодит `create`-ийн хамгийн сайн үр дүн: дүрэм олдож, бичлэг
үүсээгүй.

#### Нээлттэй үлдсэн зүйл — яамтай ярих

**«Багшийн хариуцах үүрэг» гэж юу вэ?** 162 үүнийг шаарддаг ч утгын жагсаалт
нь хаана ч алга: татсан 13 лавлахын аль нь ч агуулаагүй (`academicOrg`,
`subjectAreas` зэргийг шалгасан), 42778-ийн дөрвөн бүлгийн `instructorId`
бүгд `null` тул жишээ мөр ч байхгүй. Манай `TeacherRole` нь `LEAD | ASSISTANT`,
яамных нь тодорхойгүй.

### 1.2 Олон байгууллага — token хэрхэн хуваагддаг

2026-09-14-нд хэмжсэн: `organization/info` дээр `42778` → `200`, харин `40284`,
`42779`, `42780`, `1` → бүгд `403 «Таны компанид энэ institutionId дээр эрх
байхгүй байна»`.

Өөрөөр хэлбэл token нь **олгогдсон institution-уудын багц** авч явдаг бөгөөд
түүнээс гадуурх ID-г яам өөрөө татгалзана. Хоёр дүгнэлт:

1. Түрээслэгч хоорондын тусгаарлалтыг зөвхөн
   `Kindergarten.esisInstitutionId` барьж байгаа юм биш. Энэ бол эхний шалгалт,
   яам нь хоёр дахь нь — мөн бидний хяналтаас гадуурх учир санамсаргүй
   эвдэгдэхгүй.
2. Шинэ цэцэрлэг нэмэх нь **бичиг баримтын** алхам, кодын биш: яам тухайн
   institution-ыг энэ данс дээр нэмнэ, дараа нь superadmin баганыг тохируулна.

«Таны **компанид**» гэсэн үг нь данс нь компани бөгөөд institution-ууд түүнд
хавсрагддагийг хэлж байна. Хэрэв яам ирээдүйд цэцэрлэг бүрээс өөрийн developer
данс шаардвал ганц `ESIS_TOKEN` орчны хувьсагч хүрэлцэхгүй болж, credential нь
tenant тус бүрийн шифрлэсэн хадгалалт руу шилжинэ.

Ингэснээр `/v1/platform/kindergartens/:id/esis` нь C3-ыг `READY`, blocker-ыг
хоосон гэж мэдээлдэг болсон.

Энэ мөрийг засаж байгаа шалтгаан нь CLAUDE.md §7 өөрийгөө дахин дахин зассан
шалтгаантай нэг: кодын бодит байдалтай зөрчилдсөн баримтыг хүн уншихаа болино.

## 2. Сонгосон endpoint ба ашиглах газар

Бүх замын өмнө default-аар `https://hubv2.esis.edu.mn` орно. `institutionId`-г
backend тухайн tenant-ийн `Kindergarten.esisInstitutionId` mapping-аас нэмнэ.
Ингэснээр нэг provider token олон гэрээт цэцэрлэгийг tenant-safe байдлаар
үйлчилнэ.

### Байгууллага, хүүхэд, хүний нөөц

| API                        | Method ба зам                                            | NomadKids-д ашиглах газар                           | Одоо  |
| -------------------------- | -------------------------------------------------------- | --------------------------------------------------- | ----- |
| 59 / `API-000158`          | `GET /svc/api/hub/v2/organization/info`                  | Цэцэрлэгийн нэр, хаяг, ангилал; onboarding/settings | C1+C2 |
| 61 / `API-000160`          | `GET /svc/api/hub/v2/academic/year/statuses`             | Хичээлийн жил; бүлэг, ирц, тайлангийн filter        | C1+C2 |
| 100004874669811 / `api-40` | `GET /svc/api/hub/v2/group/list`                         | `groups`, багшийн бүлэг, ирцийн roster              | C1+C2 |
| 100004874669777 / `api-8`  | `GET /svc/api/hub/v2/students/list`                      | `children` + `enrollments` эхний import             | C1+C2 |
| 100004874669783 / `api-13` | `GET /svc/api/hub/v2/group/student/list/:studentGroupId` | Бүлгийн хүүхдийн тулгалт                            | C1+C2 |
| 100004874669778 / `2`      | `GET /svc/api/hub/v2/student/movement/v2/:beginDate`     | Элсэлт, шилжилт, гаралтын incremental sync          | C1+C2 |
| 100004874669812 / `api-41` | `GET /svc/api/hub/v2/teacher/list`                       | Багшийн membership, group assignment                | C1+C2 |
| 55 / `API-000154`          | `GET /svc/api/hub/v2/school/staff`                       | Эрхлэгч, эмч, тогооч, нягтлангийн membership        | C1+C2 |

Student/staff payload-д ESIS-ийн sample-аар email password, РД, civil-id зэрэг
талбар ирж болно. NomadKids-ийн schema эдгээрийг зориуд strip хийж, зөвхөн
`personId`, нэр, бүлэг/албан тушаалын хэрэгтэй утгыг application layer-т өгнө.

### Ирц

| API                        | Method ба зам                                                        | NomadKids-д ашиглах газар                       | Одоо  |
| -------------------------- | -------------------------------------------------------------------- | ----------------------------------------------- | ----- |
| 100004874669792 / `api-22` | `GET /svc/api/hub/v2/group/list/attendance/:studentGroupId/:dayDate` | Илгээсний дараах тулгалт, зөрүүний хяналт       | C1+C2 |
| 171 / `API-000269`         | `POST /svc/api/hub/v2/group/school/attendance/save/v3`               | `AttendanceSubmission` бүрийн өдрийн ирц илгээх | C1+C2 |

`v3` нь `institutionId`, `studentGroupId`, `dayDate`, мөн хүүхэд бүрийн
`personId`, `attendReasonCode`, `tardyMinutes`, `attendReasonList` авна. Манай
одоогийн attendance status-ыг ESIS-ийн reason code-той тулгах хүснэгт catalog-д
байхгүй тул C5 хийхээс өмнө token-той lookup/test шаардлагатай.

`/attendance/daily` дээр бүрэн бүртгэгдсэн бүлэг-өдрийг сонгоход
`POST /kindergartens/:id/attendance/daily/esis-preview` нь API-000269-ийн
илгээх body-г хүүхэд бүрийн мөрөөр бэлтгэж дэлгэцэд харуулна. Portal-ийн
гаралтын тайлбарт байгаа кодоор demo mapping нь `PRESENT`/`HALF_DAY → PRESENT`,
`EXCUSED → EXCUSED`, `SICK → SICK`, `ABSENT → UNEXCUSED`. Тодорхой ESIS
шалтгаангүй `OTHER`-ийг таамаглахгүй, илгээхийн өмнө засах алдаа болгоно.
Token байхгүй үед preview алдаа буцаана — 2026-09-14 хүртэл `demo: true`
payload үзүүлдэг байсан бөгөөд тэр нь зохиомол `institutionId`, hash-аас
гаргасан `studentGroupId`, `personId` агуулдаг байв: багш бүрэн бөгөөд
итгэмээр хүсэлт хардаг ч ЯАМ-д танигдахгүй дугаартай байлаа. Token байгаа
үед API `groups` болон `groupStudents`-ийг бодитоор дуудаж, бүлгийг нэрээр,
хүүхдийг овог нэр + төрсөн огноогоор цорын ганц тохирсон үед ESIS-ийн
`studentGroupId`, `personId`-г payload-д тавина. Олдоогүй эсвэл давхардсан
тохиолдолд буруу ID таахгүй, илгээлтийг зогсооно. `Илгээх` нь API-000269 POST
амжилттай болсны дараа л local `AttendanceSubmission` хадгална.

### Хоолны нэгдсэн лавлах

| API                | Method ба зам                                     | NomadKids-д ашиглах газар            | Одоо  |
| ------------------ | ------------------------------------------------- | ------------------------------------ | ----- |
| 111 / `API-000210` | `GET /svc/api/hub/v2/cook/product/type`           | Хоолны төрөл                         | C1+C2 |
| 112 / `API-000211` | `GET /svc/api/hub/v2/cook/materialGroup`          | Хүнсний бүлэг                        | C1+C2 |
| 123 / `API-000222` | `GET /svc/api/hub/v2/cook/material`               | `Ingredient` master data, шимт бодис | C1+C2 |
| 124 / `API-000223` | `GET /svc/api/hub/v2/cook/product`                | Хоол/бэлэн бүтээгдэхүүний лавлах     | C1+C2 |
| 125 / `API-000224` | `GET /svc/api/hub/v2/cook/productMaterials`       | `RecipeIngredient`, технологийн карт | C1+C2 |
| 126 / `API-000225` | `GET /svc/api/hub/v2/cook/kit/:productId`         | Иж бүрдлийн шимт бодис               | C1+C2 |
| 127 / `API-000226` | `GET /svc/api/hub/v2/cook/kit/product/:productId` | Иж бүрдлийн бүтээгдэхүүн             | C1+C2 |

Энэ бүлэг А/261 Хавсралт 2, СӨБ #37-ийн “хоолны нэгдсэн лавлахыг БСМС-ээс
татах” зайлшгүй шаардлагыг хэрэгжүүлэхэд зориулсан. Local `Ingredient` болон
`Recipe`-г шууд overwrite хийхгүй; external ID mapping, preview, operator
approval бүхий import C5 дээр нэмэгдэнэ.

## 2.1 Гаралтын талбарын каталог (2026-09-08 бүрэн тулгасан)

Сервис бүрийн **бүх гаралтын талбар** `esis.fields.ts`-д нэрээрээ бүртгэлтэй.
Талбар бүр `ingested: true|false` төлөвтэй; авахгүй талбар бүр `omitReason`-той
бөгөөд ямар баримтаар татгалзсаныг нэрлэнэ.

| Эх сурвалж | Сервис             | Утга                                                              |
| ---------- | ------------------ | ----------------------------------------------------------------- |
| `PORTAL`   | Сонгосон 17 сервис | 2026-09-08-нд `/api/structure`-оос нэр, төрөл, `io`-гоор тулгасан |

Нийт **256 гаралтын талбар**, attendance v3 бичих сервисийн **8 оролтын
талбар** байна. UI нь оролт, гаралтыг тусдаа badge-аар ялгана. **Demo утга
байхгүй** — 2026-09-14-нд устсан; дэлгэц нь талбарын нэр, шошго, авах/авахгүй
эсэхийг л харуулна. Token авсны дараа C4 test орчинд бодит утга, nullable
байдал, өгөгдлийн төрлийг дахин тулгана.

**Зориуд авахгүй талбарууд** (`ESIS_REQUEST.md` §1.1 (b), §1.2):
`civilId`, `personRegNumber`, `microsoftPassword`, `googlePassword`,
`microsoftEmailPass`, `googleEmailPass`, `username`. Эдгээр нь схемд огт
байхгүй — zod parse дээр хасагдана — гэхдээ
каталогид **үлдээсэн**: БМТТ-ийн шалгагч portal-той тулгахдаа "уншаад
татгалзсан" гэдгийг харах ёстой, богино жагсаалт бол алдаа мэт харагдана.

Схем ба каталогийн нийцлийг `esis.fields.test.ts` барина: `ingested` талбарын
нэрсийн олонлог зохих zod схемийн түлхүүрүүдтэй яг тэнцүү байх ёстой.

★ Суралцагчийн **нэмэлт мэдээлэл** (өрхийн байдал, эрүүл мэндийн лавлагаа, РД)
нь `ESIS_REQUEST.md` §1.1 (b) ба §1.3-аар зориуд хүсээгүй хэвээр. Дэлгэц дээр
энэ шийдвэрийг тайлбартай нь харуулна.

## 3. Кодын бэлэн хэсэг

| Файл                                                           | Үүрэг                                                                        |
| -------------------------------------------------------------- | ---------------------------------------------------------------------------- |
| `esis.endpoints.ts`                                            | Сервис бүрийн API ID, slug, method, path                                     |
| `esis.requests.ts`                                             | Порталын эрхийн хүсэлтийн бүртгэл (96 мөр) — apiId-аар каталогтой холбогддог |
| `esis.fields.ts`                                               | Сервис бүрийн бүх гаралтын талбар, авах/авахгүй шийдэл ба шалтгаан           |
| `esis.schemas.ts`                                              | ESIS envelope validation, field minimization, attendance payload             |
| `esis.service.ts`                                              | `ESIS_READERS` хүснэгт, domain method, түлхүүрээр унших `read()`             |
| `esis.client.ts`                                               | Bearer auth, timeout, safe GET retry, алдааны ангилал, token redaction       |
| `esis.service.test.ts`                                         | Endpoint path, envelope, sensitive-field stripping, v3 payload test          |
| `esis.fields.test.ts`                                          | Каталог ба схемийн тэнцэл, татгалзсан талбарын шалтгаан                      |
| `GET /v1/platform/kindergartens/:id/esis`                      | Operator-ийн readiness: token, mapping, blocker, эрхийн бүртгэл, түүх        |
| `POST /v1/platform/kindergartens/:id/esis/preview`             | Operator-ийн dry-run — 4 хүртэл GET сервис                                   |
| `GET /v1/kindergartens/:id/esis/catalog`                       | Дуудагчийн эрхээр хязгаарласан сервисийн жагсаалт                            |
| `GET /v1/kindergartens/:id/esis/resource`                      | Нэг сервисийг read-only татаж, талбар бүрийн утгыг буцаана                   |
| `GET /v1/kindergartens/:id/esis/student-registration-template` | Staff хүүхэд бүртгэлд minimized `students` contract өгнө                     |
| `GET /v1/groups/:id/attendance/esis-preview`                   | Нэг бүлэг-өдрийн API-000269 payload-ийг үүсгэнэ                              |
| `POST /v1/groups/:id/attendance/submit`                        | Багшийн бүрэн өдрийг API-000269-д илгээж, амжилтыг local хадгална            |
| `GET /v1/kindergartens/:id/esis/my-profile`                    | Нэвтэрсэн багш/ажилтны бүх зөвшөөрөгдсөн live гаралтыг харуулна              |
| `GET /v1/health/readiness`                                     | Token-ийг гаргалгүй integration config-ийн төлөв харуулна                    |

`…/esis/resource?resource=<key>&studentGroupId=…&dayDate=…` нь ADMIN эрхтэй,
tenant mapping шалгасны дараа ажиллана. Дотоод бүртгэлд юу ч бичихгүй; ганц
бичих мөр нь `AuditLog`-ийн `VIEW`. Дээд тал нь 25 мөр буцаана. Upstream
алдааг HTTP алдаа болгож биш, `status: "FAILED"` + `errorCode` болгож буцаана —
товчлуурын үүрэг нь "холболт ажиллаж байна уу?"-г хариулах явдал.

2026-09-07-нд catalog-ийн тухайн үеийн sample body-гаар 16 `GET` schema-г
автоматаар parse хийхэд бүгд амжилттай. Attendance `POST` sample нь `0` ID,
`"s"` огноотой placeholder байсан тул түүнийг бодит payload гэж зөвшөөрөөгүй;
positive ID, ISO date бүхий contract unit test-т орсон.

Тохиргоо:

```dotenv
ESIS_TOKEN=
# optional override; default нь https://hubv2.esis.edu.mn
ESIS_BASE_URL=https://hubv2.esis.edu.mn
ESIS_TIMEOUT_MS=15000
```

★★ **Demo горим 2026-09-14-нд бүрэн устсан.** Захиалагчийн заавар: «odoonoos
demo zuil ashiglahgui. buh zuil esis ees baih ystoi. data irehgui baigaa bol
teriig aldaa nii message eer haruulah».

Юу устсан бэ:

| Устсан зүйл                            | Хаана байсан    | Юу хийдэг байсан                                                                       |
| -------------------------------------- | --------------- | -------------------------------------------------------------------------------------- |
| `ESIS_DEMO_MODE`                       | `config/env.ts` | **default нь `"true"`** — тохируулаагүй байрлуулалт бүр зохиомол өгөгдөл дээр ажиллана |
| `esis.fixtures.ts` + `fixtures/*.json` | 7 файл          | Сүлжээний дуудлагыг бүхэлд нь тойрч, хуурамч envelope буцаана                          |
| `esis.samples.ts`                      | 942 мөр         | Хүснэгт бүрийн зохиомол мөрүүд                                                         |
| `EsisField.sample`                     | ~700 утга       | Талбар бүрийн зохиомол жишээ                                                           |
| `/children` дэх roster                 | 1 блок          | Бодит хүүхдүүд дээр зохиомол `personId` наагаад ESIS-ийн хариу мэт үзүүлнэ             |

Одоо нэг тээвэрлэгч: уншилт ЯАМ руу хүрнэ, эсвэл алдаа болж **хариу өгөөгүй
endpoint-ыг нэрлэнэ** (`EsisNoAnswer`). Талбарын гэрээ (`ESIS_FIELDS`) хэвээр
— тэр нь нийтлэгдсэн баримт, зохиомол утга биш.

★ Тестийн иж бүрдэлд `test/setup.ts` нь `ESIS_TOKEN`-ийг устгадаг тул ESIS
шаарддаг маршрут 502/503 буцаана. Амжилттай уншилт хэрэгтэй тест нь
`createTestApp({ esis: … })`-аар өөрөө stub хийнэ — `attendance-register.test.ts`
бол ажлын жишээ. Хаанаас ч хүрч болох нийтлэг fixture байхгүй байх нь зорилго:
яг тэр нь зохиомол өгөгдлийг дэлгэц рүү гаргасан зам байсан.

Admin evidence API нь endpoint бүрт `direction`, `targetModel`, бүх field-ийн
`mappings`, `responseMode`, `httpStatus`, `syncStatus`, `lastSyncAt` буцаана.
Resource read response нь parser-аар цэвэрлэсэн safe мөрүүдийг ESIS-ийн
`SUCCESS_CODE`, `RESPONSE_MESSAGE`, `RESULT` envelope-оор давхар буцаадаг.
Bearer token, provider password, регистр болон raw sensitive payload энэ
дэлгэцийн contract-д орохгүй.

`developerv2.esis.edu.mn` бол developer portal; API base URL биш. Token-ийг
browser руу өгөхгүй, frontend ESIS Hub-ийг шууд дуудахгүй.

Portal-ийн нүүрний cURL жишээ `hub.esis.edu.mn`, харин service catalog-ийн
endpoint бүр `hubv2.esis.edu.mn` хосттой. Хоёр хост token-гүй хүсэлтэд 401
буцааж reachable байсан ч issued token аль production host-д үйлчлэхийг C4
test-ээр баталгаажуулна. Код catalog-ийн URL болох `hubv2`-г баримталсан.

## 4. Эрх олгогдсон ч холбоогүй сервисүүд

★ **Энэ хэсгийн нэр ба үндэслэл 2026-09-14-нд өөрчлөгдсөн.** Өмнө нь
«Одоогоор зориуд **холбоогүй**» гэж нэрлээд эхний мөр нь «эмчийн тусгай эрх …
шийдэгдсэний дараа л **scope хүснэ**» гэж байв. Портал дээрх «Ашиглах
боломжтой сервисүүд» жагсаалт нь **84 сервисийг зөвшөөрөгдсөн** гэж харуулж
байгаа бөгөөд түүний дотор эрүүл мэнд, вакцин, харшил, хөгжлийн бэрхшээл бүгд
байна. Scope нь хүлээгдэж байгаа зүйл биш — **аль хэдийн олгогдсон**. Хүлээж
буй зүйл нь бидний тал: зорилгын зөвшөөрөл, retention бодлого, эмчийн эрхийн
загвар.

Ялгаа нь чухал: «эрх алга» гэдэг нь гадаад саад, «эрхтэй ч холбоогүй» гэдэг нь
бидний шийдвэр. Хоёрдугаарыг нь эхнийх мэтээр бичих нь БМТТ-г буруутай мэт
харуулна.

Кодод **39 сервис** холбогдсон, бүгд зөвшөөрөгдсөн (apiId, method, зам тулгасан
— зөрүүгүй). Үлдсэн **45** нь дараах шалтгаанаар:

- **Эрүүл мэнд, вакцин, харшил, хөгжлийн бэрхшээл, мэс засал, осол гэмтэл,
  өсөлт хөгжил, эрт илрүүлэг** (apiId 62-64, 75-82, 93-99, 108-109, 190,
  194-195): **эрхтэй**. Зорилгын зөвшөөрөл, retention хугацаа, эмчийн тусгай
  эрхийн загвар шийдэгдээгүй тул холбоогүй. NomadKids-д `growth_measurements`,
  `vaccination_records`, `allergy_records`, `safety_incidents`,
  `special_need_records` хүснэгтүүд аль хэдийн байгаа тул энэ нь **дараагийн
  шатны хамгийн том боломж**.
- **Анги бүлэг үүсгэх/засах/устгах POST** (150, 152, 162): эрхтэй. СӨБ-д буруу
  өгөгдөл үүсгэх эрсдэл нь unchanged — бүлэг устгах бол буцаах боломжгүй.
- **Мэргэшлийн зэрэг** (119, 165, 167, 170): эрхтэй. Багшийн хөгжлийн модуль
  NomadKids-д байхгүй.
- **Гэр бүл, өрхийн мэдээлэл** (73, 87): эрхтэй ч `ESIS_REQUEST.md` §1.3-аар
  зориуд хүсээгүй хэвээр — §1.1 (b)-ийн шалтгаан хүчинтэй.
- **Хоолны маягт 1/2 хадгалах** (129, 131): эрхтэй. **Унших** тал нь (130, 132)
  холбогдсон; бичих нь батлагдсан маягтын mapping хийгдээгүй тул үлдсэн.
- **Дүн, шалгалт, сурах бичиг, дүрэмт хувцас, дотуур байр**: СӨБ-ын
  NomadKids-ийн зорилгод хамаарахгүй.

## 5. C3-C5 руу шилжих checklist

1. `ESIS_REQUEST.md`-ийн API ID жагсаалтаар авах/өгөх scope батлуулах.
2. Гэрээ байгуулж `ACCESS_TOKEN`, `ENTRY_SOURCE_ID` авах.
3. Portal-ийн хэрэглэгчийн хуудсаас 17 API-ийн access status-ыг export/screenshot
   хийж change record-д хавсаргах.
4. ESIS test орчинд нэг цэцэрлэгээр GET contract test ажиллуулах.
5. Ирцийн code mapping, POST idempotency/error contract-ыг test response-оор
   батлах.
6. Урт хугацааны import-д external ID + field conflict хадгалалт, BullMQ
   dead-letter, operator approve UI нэмэх. Attendance нь одоогоор live roster-оос
   deterministic тулгалт хийдэг тул token ирмэгц ажиллана.
7. Production-д эхлээд read-only sync, дараа нь нэг цэцэрлэгийн attendance POST,
   эцэст нь шаталсан rollout хийх.

2026-09-08-ны хэрэгжилтээр `sync run/result`, tenant-safe mapping, operator
read-only preview, safe GET retry, live profile/student output, attendance live
resolve + POST хийгдсэн. Bulk import-ийн persisted external ID, field-level
conflict, approve/import, dead-letter queue нь дараагийн operational шатанд үлдсэн.

## 6. Журмын холбоос

- А/465 §3.4.3: хүсэлтэд авах/өгөх service-ийн нэрийг тусгана. API ID-тай матриц
  үүнийг нэг утгатай болгов.
- А/465 §3.7: шаардлага, service батлагдсаны дараа нэг token олгоно. C3 gate.
- А/465 §3.17: анхан шатны бүртгэлийг бодит цагийн горимоор хүргүүлнэ.
  Attendance v3 + queue нь хэрэгжүүлэх зам.
- А/465 §3.18: ашиглалтаас 3 сарын өмнө төлөвлөлт, 1 сарын өмнө test хувилбар.
- А/465 §4: эрхийг service тус бүрээр хамгийн бага хүрээнд авах ёстой.
- А/465 §5: залруулгын түүх, нотлох баримтыг хадгална. Sync audit C5-д заавал.
- А/465 §7.3: өдөр тутмын ирц, шилжилт хөдөлгөөнийг өдөр тутам бүртгэнэ.
- А/261 ерөнхий #29-31: хоёр талын солилцоо, API баримт, интеграцийн чадвар.
- А/261 СӨБ #3, #37, #51: байгууллага/ажилтан өгөгдөл өгөх-авах, хоолны
  нэгдсэн лавлах татах.

Энэ матриц нь техникийн readiness-ийг нотолно. Харин А/261-ийн бүх 94 шалгуур
хангагдсан гэсэн баталгаа биш; бүрэн өөрийн үнэлгээ `ESIS_COMPLIANCE.md`-д бий.
